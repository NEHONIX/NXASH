import { AUTH_SERVICE_ENDPOINT } from "./services/auth.endpoint.service";
import { COURSE_SERVICE_ENDPOINT } from "./services/course.endpoint.service";
import { SUBSCRIPTION_SERVICE_ENDPOINT } from "./services/subscription.endpoint.service";

export const API_ENDPOINTS = {
  auth_service: AUTH_SERVICE_ENDPOINT,
  courseService: COURSE_SERVICE_ENDPOINT,
  subscriptionService: SUBSCRIPTION_SERVICE_ENDPOINT,
};
